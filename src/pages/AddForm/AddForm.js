import React from "react";
import { AddFormForm } from "../../components";
import { Forms } from "../../components";

const AddForm = () => {
  return (
    <section className='section'>
      <AddFormForm />
      <Forms />
    </section>
  );
};

export default AddForm;
